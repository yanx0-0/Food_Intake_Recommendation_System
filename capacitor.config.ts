import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Food_Intake_Recommendation_System',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
